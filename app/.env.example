# Database Configuration
# Copy this file to .env and replace with your actual Neon PostgreSQL credentials
DATABASE_URL=postgresql://username:password@hostname/database

# Google OAuth Configuration
# Create OAuth 2.0 credentials at https://console.cloud.google.com/apis/credentials
# Authorized redirect URIs:
#   - Development: http://localhost:5178/auth/callback/google and http://[::1]:5178/auth/callback/google
#   - Production: https://[your-domain]/auth/callback/google
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Auth.js Configuration
# Generate a random secret: openssl rand -base64 32
AUTH_SECRET=your_random_secret_for_session_signing_here
AUTH_TRUST_HOST=true
